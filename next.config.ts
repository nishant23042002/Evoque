import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "picsum.photos",
      "pngimg.com",
      "cdn.shopify.com",
      "images.unsplash.com",
      "i.pinimg.com",
      "source.unsplash.com",
    ],
  },
  
};


export default nextConfig;
